[package]
name = "riverlens"
version = "0.3.0"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
poker-core = { path = "../crates/poker-core" }
serde_json = "1"
serde = { version = "1", features = ["derive"] }
riverlens-ai = { path = "../crates/riverlens-ai" }
